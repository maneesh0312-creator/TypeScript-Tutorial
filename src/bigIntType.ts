// Learning BigInt Type

// BigInt used to when failed number type
// BigInt used to banking system
// BigInt used to scientific calculator

// BigInt datatype is a special datatype which store the large integer value.
// BigInt is a primitive datatype.

// How to verify the BigInt Number
// If 2 ki power 53-1 is above, so that it is BigInt if less it is number

// How to check BigInt

console.log(Number.MAX_SAFE_INTEGER)
console.log(Number.MAX_SAFE_INTEGER + 1)
console.log(Number.MAX_SAFE_INTEGER + 2)

// How to make bigInt Number

let bigNumber:bigint = 1233483654749763n;
console.log(bigNumber);

let bigNum: bigint = BigInt(23654356373);
console.log(bigNum);

// BigInt Operation

let a = 100n;
let b = 50n;

console.log((a+b));
console.log((a*b));




